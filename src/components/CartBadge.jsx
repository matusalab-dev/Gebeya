const CartBadge = () => {
  return (
    <span className="absolute  -right-4 flex items-center justify-center text-sm rounded-[100%] size-6 bg-red text-background">
      2
    </span>
  );
};

export default CartBadge;
